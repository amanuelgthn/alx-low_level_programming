#include<stdio.h>
#include<string.h>
#include"main.h"
/**
*factorial- return the factorial of a number
*@n: number
*Return: return the factorial of the number n
**/
int factorial(int n)
{
  if (n < 0)
  {
    return (-1);
  }
  else if (n == 0)
  {
    return (1);
  }
}
