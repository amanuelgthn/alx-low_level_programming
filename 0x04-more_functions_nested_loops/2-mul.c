#include"main.h"
/**
*mul - function to multiply two numbers a and b
*@a:number 1
*@b:number 2
*Return: return the value of the multiplication of the two numbers
**/
int mul(int a, int b)
{
	int mult;

	mult = a * b;
	return (mult);
}
